# Product-First SBC Phase Lab Gap Matrix

Status: active implementation map for `product-first-sbc-phase-lab`

This is a concise implementation map for the supplied Product-First Codex
Recovery Directive. It does not certify doctrine or financial usefulness.

| Milestone | Existing reusable foundation | Product gap | Completion evidence |
| --- | --- | --- | --- |
| PFR-1 Integrated SBC workspace | `ChakraLabWorkspace`, `SbcLinkedAuditWorkspace`, Chakra snapshot and linked ledger APIs | Board and audit are split; the audit path exposes parked certification workflow | One synchronized product workspace with board, state summary, evidence, event lanes, Chakra, and plain-language Why drawer |
| PFR-2 Time/profile views | Timestamp-safe snapshot moment and existing profile metadata | Exact-event, daily, weekly, monthly, quarterly and profile-difference views remain absent | Partial: synchronized IST candle stepping/manual moment selection and active profile facts exist, but the original calendar/profile comparison contract is not yet met |
| PFR-3 USDJPY forex panel | Existing selected-aspect instrument-relative research arithmetic | The visible common-mode value is a mean of net scores, not gross common activation; joint net strength is absent | Partial/semantic correction required: readable USD/JPY context exists, but independent gross, conflict, unknown, and corrected pair formulas must be exposed before this milestone can be complete |
| PFR-4 Fixed phasor wheel | `SBC_FIXED_ZERO_PI_PHASOR_SERIES_V1` | Circular prototype has tilted fixed rays and lacks resultant, gross-ring, near-zero, unresolved, and ablation semantics | Partial: fixed scalar data and vector selection are useful foundations, but the visible wheel does not yet meet the original fixed-geometry contract |
| PFR-5 Isolated timing experiment | No directional phase engine exists | V0 rotates the scalar baseline with one nearest window instead of deriving independent active-event phases | Prototype: guardrails and a non-voting display exist, but per-event asymmetric lifecycle geometry and backend authority are required |
| PFR-6 Model comparison | Existing scalar ledger and fixed visualization | No single comparison view | Complete: a shared-timestamp comparison keeps scalar SBC, fixed 0/pi parity, and the isolated timing experiment in separate columns, exposes why they differ, shows evidence cutoff/no-future wording, and retains ABSTAIN/no-vote labels |
| PFR-7 Windows beta | Tauri desktop build pipeline | No focused founder acceptance package | Candidate built: 0.10.25 installer and self-contained portable folder hash-verified; portable native window/backend launch passed. Founder walkthrough remains the explicit acceptance checkpoint before any new work. |

## Parked on this Branch

- Shadbala and Drik source reconciliation expansion.
- Source certification, signatures, reviewer or authority workflows.
- Auto Suggest, official ML notes, shadow validation, live inference, trade,
  MT5, Android, RAG, and speculative field research.

## Invariants

Every PFR milestone keeps `executionAllowed=false`,
`automaticOrderPlacement=false`, no-lookahead behavior, explicit unknowns,
and the existing scalar evidence baseline. Any experimental timing display is
nonvoting and cannot generate market direction, financial confidence, or an
order.

## PFR-C1 Correction Status

PFR-C1 is the single bounded integrity pass defined by
`ADR-0018-pfr-c1-product-integrity-correction.md`. It preserves the original
PFR definitions, corrects visible mathematical/semantic gaps, and stops after
a corrected Windows candidate and founder acceptance. It does not authorize
new certification, research, ML, trading, Android, or execution work.
