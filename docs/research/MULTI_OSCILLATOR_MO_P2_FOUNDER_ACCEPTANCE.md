# Multi Oscillator MO-P2 Founder Acceptance

Status: `FOUNDER ACCEPTED`

Acceptance date: `2026-08-25`

## Accepted Baseline

`MO-P2 = FOUNDER ACCEPTED - UNSIGNED EVENT ACTIVITY V0`

`MO-P2-F1-R1 = FOUNDER ACCEPTED`

Founder candidate: `0.10.60-pfr-v2b-mo-p2-f1-r1`

This record durably records the founder's physical inspection of the packaged
Windows activity product. The candidate and its source remain immutable. The
prior candidate report remains the historical implementation record; this note
records the later acceptance decision.

## Physical Checks

The founder verified:

- real USD event spans are visible;
- real JPY event spans are visible;
- exact event markers are visible;
- the shared raw activity scale works;
- disabling and re-enabling aspect filters removes and restores events;
- the shared scale recomputes from visible events;
- raw-count interval interaction works;
- `UNKNOWN` coverage remains separate from observed activity magnitude;
- the packaged Windows runtime operates with execution locked.

## Meaning and Limits

This acceptance is limited to the unsigned event-activity product. It is not:

- directional validation;
- financial validation;
- signal or forecast validation;
- polarity validation;
- evidence for a signed wave or pair resultant.

The accepted contract remains read-only, exploratory and non-predictive. It
does not authorize USD-JPY subtraction, signed contributions, magnitude,
smoothing, interpolation, price or outcome reads, SBC/CGVO/BPHS fusion, LLM or
ML inference, Auto Suggest, MT5 signals, order placement or execution.

`executionAllowed = false`

The next milestone, MO-P3A, may productize the same discrete counts as exact
unsigned step waves, but must preserve these limits.
